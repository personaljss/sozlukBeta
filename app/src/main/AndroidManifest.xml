<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yusufemirbektas.sozlukBeta">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sozluk_2"
        tools:targetApi="31">
        <activity
            android:name=".mainApplication.profile.activity.ProfileActivity"
            android:exported="true"
            android:parentActivityName=".mainApplication.MainActivity"/>
        <activity
            android:name=".loginPage.activities.activation.ActivationActivity"
            android:exported="false"
            android:parentActivityName=".loginPage.activities.login.LoginActivity" />
        <activity
            android:name=".mainApplication.MainActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>
        <activity
            android:name=".loginPage.activities.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>